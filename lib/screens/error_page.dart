import 'package:flutter/material.dart';

class ErrorPage extends StatelessWidget {
  final String errorMsg;
  // ignore: use_key_in_widget_constructors
  const ErrorPage(this.errorMsg);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
